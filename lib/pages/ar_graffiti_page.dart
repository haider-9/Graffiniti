import 'package:flutter/material.dart';
import '../ui/ar_stickers/widgets/ar_stickers_screen.dart';

class ARGraffitiPage extends StatelessWidget {
  const ARGraffitiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const ARStickersScreen();
  }
}
